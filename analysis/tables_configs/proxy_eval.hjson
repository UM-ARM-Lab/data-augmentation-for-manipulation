[
  {
    type: MyTable
    name: all
    header: [
      Classifer
      Aug?
      Classifier Source Env
      Fine-Tuning Seed
      Random Actions Specificity
    ]
    reductions: [
      [
        [["uuid"], "classifier", "first"]
      ]
      [
        [["uuid"], "do_augmentation", "first"]
      ]
      [
        [["uuid"], "classifier_source_env", "first"]
      ]
      [
        [["uuid"], "fine_tuning_seed", "first"]
      ]
      [
        [["uuid"], "accuracy on negatives", "mean"]
      ]
    ]
  }
  {
    type: MyTable
    name: mean
    header: [
      Classifier Source Env
      Aug?
      Fine-Tuning Take
      Random Actions Specificity
    ]
    reductions: [
      [
        [["do_augmentation", "fine_tuning_take", "classifier_source_env", "dataset_dirs", "mode", "balance"], "classifier_source_env", "first"]
      ]
      [
        [["do_augmentation", "fine_tuning_take", "classifier_source_env", "dataset_dirs", "mode", "balance"], "do_augmentation", "first"]
      ]
      # [
      #   [["do_augmentation", "fine_tuning_take", "classifier_source_env", "dataset_dirs", "mode", "balance"], "aug", "first"]
      # ]
      [
        [["do_augmentation", "fine_tuning_take", "classifier_source_env", "dataset_dirs", "mode", "balance"], "fine_tuning_take", "first"]
      ]
      [
        [["do_augmentation", "fine_tuning_take", "classifier_source_env", "dataset_dirs", "mode", "balance"], "accuracy on negatives", "mean"]
      ]
    ]
  }
]